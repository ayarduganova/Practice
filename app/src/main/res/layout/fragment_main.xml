<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainFragment">

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="240dp"
        android:text="Main Screen"
        android:textSize="32sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
    <Button
        android:id="@+id/btnToProfile"
        android:layout_width="match_parent"
        android:text="GoToProfile"
        android:layout_marginTop="16dp"
        android:layout_marginHorizontal="16dp"
        app:layout_constraintTop_toBottomOf="@+id/tvTitle"
        android:layout_height="wrap_content"/>
    <Button
        android:id="@+id/btnToSettings"
        android:layout_width="match_parent"
        android:text="GoToSettings"
        android:layout_marginTop="16dp"
        android:layout_marginHorizontal="16dp"
        app:layout_constraintTop_toBottomOf="@+id/btnToProfile"
        android:layout_height="wrap_content"/>
    <Button
        android:id="@+id/btnToMusic"
        android:layout_width="match_parent"
        android:text="GoToMusic"
        android:layout_marginTop="16dp"
        android:layout_marginHorizontal="16dp"
        app:layout_constraintTop_toBottomOf="@+id/btnToSettings"
        android:layout_height="wrap_content"/>
    <Button
        android:id="@+id/btnToGroups"
        android:layout_width="match_parent"
        android:text="GoToGroups"
        android:layout_marginTop="16dp"
        android:layout_marginHorizontal="16dp"
        app:layout_constraintTop_toBottomOf="@+id/btnToMusic"
        android:layout_height="wrap_content"/>

</androidx.constraintlayout.widget.ConstraintLayout>